% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leviFromDE.R
\name{leviFromEdgeR}
\alias{leviFromEdgeR}
\title{leviFromEdgeR}
\usage{
leviFromEdgeR(fit, coef = 1, gene_col = "GeneID")
}
\arguments{
\item{fit}{A \code{DGELRT} or \code{DGEExact} object from edgeR, or any
\code{data.frame} with columns \code{logFC} and \code{logCPM}.}

\item{coef}{Retained for call compatibility. Omit for edgeR test results;
select the coefficient or contrast in \code{glmLRT()} or
\code{glmQLFTest()} before calling this adapter.}

\item{gene_col}{Character. Name for the gene identifier column.
Default is \code{"GeneID"}.}
}
\value{
A \code{data.frame} with columns: gene identifier, logCPM
(abundance annotation) and logFC. Use \code{signal_mode = "logfc"}
with \code{readExpColumn("logFC-logFC")}; abundance is not a control.
}
\description{
Convert an edgeR result object into a data.frame compatible
with \code{levi}'s \code{expressionInput} parameter.
}
\examples{
mock_tt <- data.frame(
    logFC  = c(2.1, -1.4, 0.5),
    logCPM = c(5.2, 3.8, 4.1),
    row.names = c("GENE1", "GENE2", "GENE3")
)
expr_df <- leviFromEdgeR(mock_tt)
head(expr_df)
\dontrun{
library(edgeR)
fit <- glmQLFit(dge, design)
qlf <- glmQLFTest(fit, coef = 2)
expr_df <- leviFromEdgeR(qlf, gene_col = "GeneSymbol")
}
}
