% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leviInference.R
\name{leviGraphClusterInference}
\alias{leviGraphClusterInference}
\title{Graph-connected cluster-mass inference from biological replicates}
\usage{
leviGraphClusterInference(
  expression,
  groups,
  networkCoordinatesInput,
  networkInteractionsInput = NA,
  fileTypeInput = "dat",
  test = unique(groups)[2],
  control = unique(groups)[1],
  threshold = 2,
  n_perm = 999L,
  blocks = NULL,
  seed = NULL,
  permutation_method = c("auto", "monte_carlo", "exact"),
  max_exact = 50000L,
  BPPARAM = SerialParam()
)
}
\arguments{
\item{expression}{Log-scale genes-by-samples expression matrix.}

\item{groups}{Two-level sample-group vector.}

\item{networkCoordinatesInput, networkInteractionsInput, fileTypeInput}{LEVI
network inputs.}

\item{test, control}{Group labels defining test minus control.}

\item{threshold}{Absolute moderated-t threshold for cluster formation.}

\item{n_perm}{Number of sample-label permutations.}

\item{blocks}{Optional exchangeability blocks.}

\item{seed}{Optional RNG seed.}

\item{permutation_method}{`"auto"`, `"monte_carlo"` or `"exact"`.
`"exact"` enumerates every distinct label assignment; `"monte_carlo"`
draws `n_perm` random permutations; `"auto"` uses the exact scheme when
the number of assignments is at most `max_exact` and Monte Carlo otherwise.}

\item{max_exact}{Maximum number of label assignments enumerated by the exact
permutation scheme.}

\item{BPPARAM}{A [BiocParallel::BiocParallelParam] object controlling how
the permutations are evaluated. The default runs them serially.}
}
\value{
List with observed moderated statistics, graph regions and null maxima.
}
\description{
Fits a moderated `limma` t-statistic for every gene, thresholds the signed
statistics, forms components using biological network edges, and assigns
FWER-controlled P-values by permuting sample labels. The graph components,
rather than adjacency on the 2D layout grid, are the inferential regions.
}
\examples{
if (requireNamespace("limma", quietly = TRUE)) {
  set.seed(1)
  genes <- c("HUB", paste0("N", 1:8))
  # Five paired donors: 32 within-donor label swaps, enumerated exactly.
  expression <- matrix(rnorm(length(genes) * 10, sd = .1), length(genes), 10,
    dimnames = list(genes, NULL))
  expression[, 6:10] <- expression[, 6:10] + 2
  out <- leviGraphClusterInference(expression, rep(c("C", "T"), each = 5),
    system.file("extdata", "hub_network.dat", package = "levi"),
    test = "T", control = "C", threshold = 1,
    blocks = rep(paste0("d", 1:5), 2), seed = 1)
  out$regions$summary
}
}
