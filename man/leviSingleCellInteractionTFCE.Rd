% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leviSingleCellStatistics.R
\name{leviSingleCellInteractionTFCE}
\alias{leviSingleCellInteractionTFCE}
\title{Cell-type-specific condition interaction with joint graph TFCE inference}
\usage{
leviSingleCellInteractionTFCE(
  counts,
  donor,
  cell_type,
  condition,
  networkCoordinatesInput,
  networkInteractionsInput = NA,
  fileTypeInput = "dat",
  reference_cell_type = NULL,
  min_cells = 10L,
  cell_types = NULL,
  n_perm = 999L,
  permutation_method = c("auto", "monte_carlo", "exact"),
  max_exact = 50000L,
  E = 0.5,
  H = 2,
  n_steps = 100L,
  seed = NULL,
  normalize = c("TMM", "none"),
  BPPARAM = SerialParam()
)
}
\arguments{
\item{counts}{Genes-by-cells raw count matrix with gene row names.}

\item{donor, cell_type}{One donor and cell-type label per cell.}

\item{condition}{Two-level condition label per cell; every donor must
contribute cells to both levels within each analysed cell type.}

\item{networkCoordinatesInput, networkInteractionsInput, fileTypeInput}{LEVI
network inputs: node coordinates, optional interactions and the file type
accepted by [levi()].}

\item{reference_cell_type}{Cell type against which the condition effect of
every other type is contrasted. Defaults to the first complete type.}

\item{min_cells}{Minimum cells in each donor-condition-cell-type pseudobulk.}

\item{cell_types}{Optional cell types to analyse; at least two complete
types are required.}

\item{n_perm}{Number of Monte Carlo within-donor label permutations.}

\item{permutation_method}{`"auto"`, `"monte_carlo"` or `"exact"`.
`"exact"` enumerates every distinct label assignment; `"monte_carlo"`
draws `n_perm` random permutations; `"auto"` uses the exact scheme when
the number of assignments is at most `max_exact` and Monte Carlo otherwise.}

\item{max_exact}{Maximum number of label assignments enumerated by the exact
permutation scheme.}

\item{E, H}{TFCE extent and height exponents applied to the component size
and to the threshold height at every integration step.}

\item{n_steps}{Number of threshold steps used to integrate the TFCE score
between zero and the maximum statistic.}

\item{seed}{Optional RNG seed.}

\item{normalize}{Library-size normalisation applied before the log2
counts-per-million transform: `"TMM"` (trimmed mean of M-values via
\pkg{edgeR}, the default) or `"none"` (plain counts per million).}

\item{BPPARAM}{A [BiocParallel::BiocParallelParam] object controlling how
the permutations are evaluated. The default runs them serially.}
}
\value{
A list of interaction TFCE tables and the joint donor-label null
  distribution.
}
\description{
Tests whether the condition effect in each non-reference cell type differs
from the reference type. Labels are exchanged within donors jointly over all
cell types, avoiding cell-level pseudoreplication.
}
\examples{
if (requireNamespace("limma", quietly = TRUE)) {
  set.seed(1)
  genes <- c("HUB", paste0("N", 1:8))
  donor <- rep(paste0("d", 1:5), each = 4)
  condition <- rep(rep(c("ctrl", "stim"), each = 2), 5)
  cell_type <- rep(c("A", "B"), times = 10)
  counts <- matrix(rpois(length(genes) * length(donor), 20), length(genes),
    dimnames = list(genes, NULL))
  out <- leviSingleCellInteractionTFCE(counts, donor, cell_type, condition,
    system.file("extdata", "hub_network.dat", package = "levi"),
    reference_cell_type = "A", min_cells = 1, n_perm = 19,
    permutation_method = "monte_carlo", seed = 1)
  out$results$B
}
}
