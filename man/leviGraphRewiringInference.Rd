% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leviNetworkStatistics.R
\name{leviGraphRewiringInference}
\alias{leviGraphRewiringInference}
\title{Degree-preserving rewiring test for graph clusters}
\usage{
leviGraphRewiringInference(
  scores,
  networkCoordinatesInput,
  networkInteractionsInput = NA,
  fileTypeInput = "dat",
  threshold = 2,
  n_perm = 999L,
  rewire_steps = NULL,
  seed = NULL,
  BPPARAM = SerialParam()
)
}
\arguments{
\item{scores}{Named numeric vector of gene scores (for example moderated
t-statistics or log fold-changes) indexed by gene identifier. Network
nodes without a finite score are dropped before testing.}

\item{networkCoordinatesInput, networkInteractionsInput, fileTypeInput}{LEVI
network inputs: node coordinates, optional interactions and the file type
accepted by [levi()].}

\item{threshold}{Absolute score threshold for cluster formation.}

\item{n_perm}{Number of rewired null networks.}

\item{rewire_steps}{Number of degree-preserving edge swaps passed to
[igraph::keeping_degseq()] for every null network. Defaults to ten times
the number of edges (at least 10).}

\item{seed}{Optional RNG seed.}

\item{BPPARAM}{A [BiocParallel::BiocParallelParam] object controlling how
the permutations are evaluated. The default runs them serially.}
}
\value{
A list of observed graph regions, degree-preserving rewiring null
  maxima and topology P-values.
}
\description{
Thresholds gene scores, forms connected components on the biological
network and compares the observed maximum cluster mass with the mass
obtained on degree-preserving rewired networks. This tests whether the
clustering depends on the specific wiring rather than on the degree
sequence.
}
\examples{
network <- data.frame(ID = c("A", "B", "C", "D"), X = 1:4, Y = 1:4)
edges <- data.frame(V1 = c("A", "B", "C", "D"), V2 = c("B", "C", "D", "A"))
leviGraphRewiringInference(c(A = 3, B = 2.5, C = -1, D = -2), network, edges,
  fileTypeInput = "stg", n_perm = 9)
}
