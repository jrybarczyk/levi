% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/leviInference.R
\name{leviReplicateInference}
\alias{leviReplicateInference}
\title{Regional inference from biological replicates}
\usage{
leviReplicateInference(
  expression,
  groups,
  gene_ids = rownames(expression),
  test = unique(groups)[2],
  control = unique(groups)[1],
  blocks = NULL,
  n_perm = 999L,
  sig_level = 0.05,
  seed = NULL,
  permutation_method = c("auto", "monte_carlo", "exact"),
  max_exact = 50000L,
  BPPARAM = SerialParam(),
  ...
)
}
\arguments{
\item{expression}{Numeric genes-by-samples matrix on a log scale.}

\item{groups}{Two-level condition vector, one value per sample.}

\item{gene_ids}{Gene identifiers; defaults to row names.}

\item{test, control}{Levels defining the `test - control` contrast.}

\item{blocks}{Optional permutation blocks (e.g. donor identifiers).}

\item{n_perm}{Number of label permutations.}

\item{sig_level}{Significance level.}

\item{seed}{Optional RNG seed.}

\item{permutation_method}{`"auto"`, `"monte_carlo"` or `"exact"`.
`"exact"` enumerates every distinct label assignment; `"monte_carlo"`
draws `n_perm` random permutations; `"auto"` uses the exact scheme when
the number of assignments is at most `max_exact` and Monte Carlo otherwise.}

\item{max_exact}{Maximum number of label assignments enumerated by the exact
permutation scheme.}

\item{BPPARAM}{A [BiocParallel::BiocParallelParam] object controlling how
the permutations are evaluated. The default runs them serially.}

\item{...}{Network and landscape arguments passed to [levi()]. Do not supply
expression, comparison, signal mode, permutation, or inference arguments.}
}
\value{
A `levi_result` whose regional P-values are based on sample labels.
}
\description{
Permutes sample labels, recalculates gene log fold-changes, reconstructs the
complete LEVI landscape and redetects regions at every iteration. This tests
treatment association across biological replicates rather than node-label
localisation on one fixed effect vector.
}
\examples{
set.seed(1)
genes <- c("HUB", paste0("N", 1:8))
# Four replicates per group: 70 label arrangements, enumerated exactly.
expression <- matrix(rnorm(length(genes) * 8, sd = .1), length(genes), 8,
  dimnames = list(genes, NULL))
expression[, 5:8] <- expression[, 5:8] + 1
out <- leviReplicateInference(expression, rep(c("C", "T"), each = 4),
  test = "T", control = "C", seed = 2,
  networkCoordinatesInput = system.file("extdata", "hub_network.dat", package = "levi"),
  fileTypeInput = "dat", resolutionValueInput = 1, region_threshold = .02)
out$regions$summary
}
